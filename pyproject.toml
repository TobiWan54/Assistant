[project]
name = "assistant"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "qwen_agent",
    "numpy",
    "soundfile",
    "python-dateutil",
    "tzdata",
    "openai",
    "mcp"
]
